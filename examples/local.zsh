# Machine-specific paths, aliases, and functions belong here.
# Copy to local/zsh/local.zsh, then rerun init.sh.

# export ANDROID_HOME="$HOME/Library/Android/sdk"
# alias setup="$HOME/.local/bin/tmux-setup"
